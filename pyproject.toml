[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ofrendas"
version = "0.1.0"
description = "Ofrendas application"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["offering_app", "offering_app.*"]